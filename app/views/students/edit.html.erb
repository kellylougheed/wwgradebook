<h1>Edit Student</h1>

<%= simple_form_for @student, url: student_path(@student) do |f| %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.submit "Update Student", class: 'btn btn-primary' %>
<% end %>
